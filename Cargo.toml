[package]
name = "pg-starttls"
version = "0.0.1"
edition = "2024"

[dependencies]
humantime = "2"
socket2 = "0.6"
tokio = { version = "1", features = ["io-util", "macros", "net", "rt-multi-thread", "signal", "sync", "time"] }

[profile.release]
codegen-units = 1
lto = true
strip = true
